# Copy this file to .env for local testing
# NEVER commit the real .env to GitHub
# On Render, set MONGO_URI in the Environment tab instead

MONGO_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/cbt_portal?retryWrites=true&w=majority
